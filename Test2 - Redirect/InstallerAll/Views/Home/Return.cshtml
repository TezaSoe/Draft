
@{
    ViewData["Title"] = "Return";
}

@if (User.Identity.IsAuthenticated)
{
    var guid = Guid.NewGuid().ToString();
    //Html.Assets().Styles.Add("/css/site.css?v=" + guid);
    Html.Assets().Scripts.Add("/js/return.js?v=" + guid);
}